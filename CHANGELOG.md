# Changelog

## v0.5.1 (2026-09-22)

- 变更：平台名称映射改为 list 列表，每项格式 `原始平台名=显示名`（如 `aiocqhttp=QQ`），减少设置项长度占用过长问题，全部平台映射均在 WebUI 中可视化管理

## v0.5.0 (2026-09-21)

- 新增：内容类型筛选：可勾选需要转发的类型（文字/图片/表情/语音/视频/文件/@提及/其他），未勾选的类型在转发前被过滤，纯媒体转发时不再附带来源信息头
- 新增：`@` 转发——`At` 组件保留 `qq` 与 `name` 原样透传，由目标协议端精确解析，仅当 `qq` 为空时才降级为文本 `@昵称`。
- 新增：`@` 昵称反查（可选增强，**默认关闭**）：开启后转发前按目标群成员列表把 `@昵称` 反查为目标群真实 `qq` 再精确 @，反查顺序为「qq 精确匹配优先，昵称兜底」
- 新增：反查安全与性能保护：群成员列表按群缓存，昵称重名时跳过反查以免 @ 错人，拉取失败/无 QQ 协议端/目标非 QQ 群时静默降级，不影响转发
- 新增：`/mf at` 命令组：`at` 查看 @ 转发与反查配置、`at on|off` 全局开关、`at <编号> on|off|inherit` 规则级开关、`at cache <秒>` 设置群成员缓存时长、`at test <群号|UMO>` 实测目标群反查命中情况
- 变更：`/mf list` / `/mf listall` 为开启昵称反查的规则增加 `🔔@反查` 标记
- 新增：`/mf content` 命令：查看内容类型筛选配置
- 变更：`/mf list` / `/mf listall` 为配置了内容类型覆盖的规则增加 `📦` 标记（仅 1 类时显示类型名如 📦仅表情，多类显示类数如 📦3类）
- 变更：WebUI 选项全部本地化为中文显示，存储值保持英文键不变，旧配置完全兼容
- 提示：QQ中的「表情」仅指 QQ 内置表情（`Face` 组件），收藏的表情/表情包以图片形式到达，统一归入「图片」类型

## v0.4.7 (2026-09-19)

- 变更：相关命令全部限定为管理员权限（除了 help 为普通权限）
- 新增：`/mf remark <编号> <备注>` 设置规则备注名称（留空清除备注，恢复默认显示），与规则 `remark` 字段联动
- 新增：`/mf cooldown` 命令：查看当前冷却配置、`/mf cooldown default <秒>` 设置全局默认冷却（0=关闭，`default` 前缀与规则编号区分避免混淆、兼容无前缀旧用法）、`/mf cooldown <编号> <秒|inherit>` 设置规则级冷却（inherit=重置为继承全局），无需再进 WebUI 修改冷却时间
- 变更：`/mf help` 改为按功能分类的树状结构——主帮助按「绑定 / 规则 / 冷却 / 过滤 / 发送队列」分组展示全部命令（含冷却与队列子命令树）

## v0.4.6 (2026-09-18)

- 变更：抽取重复的辅助函数，统一骨架，消除重复代码
- 变更：`/mf help` 中发送队列指令改为树状目录形式汇总（`/mf queue` 一条入口 + `/mf queue help` 查看详情），其余指令展示保持不变
- 新增：`/mf queue` 命令组，方便通过命令管理队列（作为 `mf` 的子指令组，避免顶层 `queue` 指令冲突），发送队列配置支持指令操作：`/mf queue status` 查看状态、`on/off` 总开关、`interval` 全局默认间隔、`maxsize` 队列上限、`retention` 媒体缓存保留时长、`set <编号> <秒>` 规则级间隔、`clear` 清空积压队列
- 变更：移除 `/mf hidelist` 与 `/mf hidelistall` 命令（来源信息状态已由 `/mf list` / `/mf listall` 的 🔒/🔓 标记覆盖）
- 新增：发送队列持久化：入队消息同步写入 `queue.json`，AstrBot 重启/插件重载后自动恢复未发送完的消息，避免积压消息丢失
- 变更：队列媒体缓存目录由系统临时目录改为 AstrBot data 目录下（`astrbot/data/plugin_data/msg_forward_cc/media/`），重启/重载不丢文件，配合队列持久化重启后媒体仍可重新发送

## v0.4.5 (2026-08-19)

- 修复：增强媒体本地化失败降级逻辑，失败时自动尝试从远程URL下载重建，解决群文件转发「源文件不可达」问题
- 修复：放宽发送失败自动降级触发条件，无远程URL的本地文件支持本地文件兜底重建后重试
- 修复：重建File/Video优先使用原始URL而非本地路径，调整File/Video组件本地路径，队列/即时转发场景下仅保留URL
- 修复：无URL的群文件File组件调用API获取下载URL，解决容器内群文件无法通过本地路径重新上传的问题

## v0.4.4 (2026-08-18)

- 新增：队列功能全局开关与缓存保留时间设置
- 新增：入队前将媒体本地化到插件自有临时目录，确保队列延迟后文件仍可访问
- 修复：发送失败时自动降级发送

## v0.4.3 (2026-08-17)

- 新增：发送队列等待功能：规则可设置发送队列间隔，开启后匹配的消息不立即转发，而是进入队列，由后台每隔设定的秒数依次转发一条
- 新增：发送队列最大长度：达到上限后新消息丢弃（记录日志），队列中已有消息不受影响；默认 0=不限制
- 改进：`/mf list` / `/mf listall` 增加 `⏳` 队列间隔标记，`/mf filter` 增加发送队列配置展示（含队列上限）

## v0.4.2 (2026-08-16)

- 新增：规则级媒体下载代理三态配置（use_proxy / proxy_url）：关闭直连；开启且地址留空走 AstrBot 自带代理；开启且填写则走该地址

## v0.4.1 (2026-08-16)

- 新增：媒体转发失败自动降级：发送失败时自动下载远程媒体到本地重试

## v0.4.0 (2026-08-13)

- 新增：转发规则启用开关：每条规则新增启用开关，关闭后规则保留但不再转发消息
- 新增：转发规则支持自定义备注作为规则的展示名称
- 新增：多源多目标规则：可填多个来源于转发会话（每行一条），消息会转发到目标列表中的每个会话
- 改进：平台名称映射默认映射扩展：新增 AstrBot 全部官方适配器的来源信息中的显示名称
- 改进：单独配置 `发送媒体前先下载到本地`：每条转发规则可单独设置是否在发送媒体前先下载到本地，未设置时继承全局配置
- 修复：转发前清洗无效的 `@` 提及组件：空目标直接丢弃，非数字目标（如 openid、`qq_official`）降级为纯文本 `@昵称`，仅保留 `@全体成员`（`all`）与纯数字目标；避免目标平台（如 OneBot/NapCat）用空 uid 查询群成员导致内核调用超时（`retcode=1200 invoke timeout`），使整个转发失败

## v0.3.0 (2026-07-06)

- 新增：媒体本地化转发配置项 `发送媒体前先下载到本地`：开启后转发前将 Image/Record/Video/File 组件重下载到本机临时目录并以 `fromFileSystem` 重建，解决跨会话转发时源端临时路径不可达（ENOENT）问题；失败时降级为占位文本
- 新增：新插件 logo

## v0.2.1 (2026-06-18)

- 新增：`/mf bindraw` 直接建规则：支持平台简写（df/qq/wx/tg/dc），超过 3 个字母的平台名直接作为完整平台标识，平台/会话 ID 末尾加 `s` 表示私聊（FriendMessage）
- 新增：消息过滤系统：支持关键词（不区分大小写包含匹配）与正则（`regex:` 前缀）
- 新增：转发冷却，冷却期内不重复转发

## v0.2.0 (2026-06-17)

- 新增：来源信息控制：支持隐藏转发来源信息头
- 新增：WebUI 可视化配置
- 新增：指令集更新：完善 `/mf help` 帮助信息
- 变更：移除 `requirements.txt`：改为纯标准库依赖

## v0.1.0 (2026-05-01)

**fork 自**：[Siaospeed/astrbot_plugin_msg_transfer](https://github.com/Siaospeed/astrbot_plugin_msg_transfer)

- 新增：跨平台消息转发：通过转发规则将消息从 A 会话同步到 B 会话
- 新增：绑定码绑定流程：`add` 生成绑定码，目标会话 `bind` 接受后创建转发规则
- 新增：消息来源标注：转发时自动添加来源信息头
